example 1


/*
 * saving numbers to a text file
 */

#include <stdio.h>

int
main ()
{
	int	i, x;
	FILE *fp;

	fp = fopen ("file", "w");

	for (i = 0; i < 10; i++)
	{
		scanf ("%d", &x);
		fprintf (fp, "%d\n", x);
	}

	fclose (fp);

}